# ECOYA 작업 범위와 변경 원칙

## 구조

이 저장소는 React + TypeScript + Vite 앱 하나입니다. 폴더는 수정 책임을 나눈 것이며 빌드·배포가 독립된 앱은 아닙니다.

| 영역 | 위치 | 책임 |
| --- | --- | --- |
| 공개 홈페이지 | `landing/` | 브랜드 스토리, 제품 소개, 가격, 도입 문의 |
| 인증 | `auth/` | 로그인, 회원가입, 계정 복구, 인증 레이아웃 |
| Trade OS 업무 | `trade-os/` | `/erp/*` 업무 화면과 로직 |
| SNAP 업무 | `snap/` | 현장 업무 화면과 로직 |
| 공통 UI | `packages/shared-ui/`, `packages/ecoya-ui/` | 여러 영역이 소비하는 컴포넌트·훅·유틸리티 |
| 디자인 시스템 | `packages/design-system-2.0/` | 공통 토큰·브랜드 자산 |
| 앱 연결 | `src/` | 진입점, 라우팅, 전역 스타일 |

## 한 페이지 요청은 해당 페이지에 한정

- 사용자가 홈 또는 특정 페이지 수정을 요청하면 그 페이지의 문구·이미지·배치·전용 스타일만 변경합니다. 다른 페이지의 디자인을 함께 바꾸는 것으로 해석하지 않습니다.
- 홈페이지 작업 전 `landing/AGENTS.md`를 읽고 실제 import와 CSS 소비 범위를 확인합니다.
- 홈 디자인을 맞추려고 공통 Button/Input, 전역 토큰, `src/index.css`, 업무 코드 또는 인증 동작을 바꾸지 않습니다. 페이지 전용 컴포넌트·래퍼·스타일로 해결합니다.
- 공통 파일 안에서도 해당 페이지 전용 규칙만 수정할 수 있습니다. 파일 위치만으로 영향 범위를 판단하지 않습니다.
- 공통 변경이 명시적으로 요청된 경우에는 소비 페이지를 확인한 후 필요한 범위만 수정하고 검증합니다. 기존 지시로 범위가 분명하면 같은 승인을 다시 묻지 않습니다.
- API 주소, 로그인·가입·시작하기 링크, 라우팅, 결제·인증 로직은 문구·이미지·레이아웃 수정의 범위에 포함하지 않습니다.
- 공용 이미지·로고 파일을 덮어쓰면 여러 화면이 바뀝니다. 한 페이지용 이미지는 새 이름으로 저장해 해당 페이지에서만 참조합니다.

## 검증과 보고

- 코드·스타일 변경 시 `npm run build`와 변경 내용에 맞는 기존 검사를 실행합니다. 지침·문서만 바꿀 때는 불필요한 앱 테스트를 추가하지 않습니다.
- 페이지 전용 변경은 해당 페이지의 데스크톱·모바일을 확인합니다. 공통 CSS·컴포넌트를 바꿨다면 실제 소비 페이지도 확인합니다.
- 검증하지 않은 페이지까지 영향이 없다고 단정하지 않습니다. 최종 보고에 수정 범위와 확인한 범위를 적습니다.
- 관련 없는 로컬 변경을 커밋에 섞거나 덮어쓰지 않습니다.

## 배포 연결

- 원격 저장소: `https://github.com/halo-hs/devdev`
- Cloudflare Pages 프로젝트: `devdev`
- 운영 주소: `https://devdev-e6t.pages.dev`
- Git 직접 연동: `main` push → `npm run build` → `dist` 배포.
- GitHub Actions나 별도 배포 토큰은 현재 직접 연동에 필요하지 않습니다. 다른 배포 파이프라인을 임의로 추가하지 않습니다.
